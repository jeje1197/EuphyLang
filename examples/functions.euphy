// A few functions are available to you out of the box: print, type, input, etc.
// But you can also create your own using the following format
// return_type function_name(args...) {
//    Your code goes here
// }

number addNums(number a, number b) {
    return a + b
}

number result = addNums(12, 24)
print(result)

// functions can be passed as values
dynamic addNumbers = addNums

result = addNumbers(92, 5)
print(result)